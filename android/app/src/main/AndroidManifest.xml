<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".SampleApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Sloth">
        <activity
            android:name=".LongSlothActivity"
            android:exported="true"
            android:label="LongSloth"
            android:taskAffinity=".LongSlothActivity"
            android:theme="@style/Theme.Sloth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HiddenSlothActivity"
            android:exported="true"
            android:label="HiddenSloth"
            android:taskAffinity=".HiddenSlothActivity"
            android:theme="@style/Theme.Sloth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
